# GitHub Personal Access Token (get from https://github.com/settings/tokens)
# Leave empty for public API (60 requests/hour limit)
# Add token for authenticated API (5000 requests/hour limit)
GITHUB_TOKEN=your_token_here

# Server port
PORT=5000
